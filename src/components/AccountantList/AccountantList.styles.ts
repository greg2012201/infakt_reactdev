import { styled } from "styled-components";

export const List = styled.ul`
    display: flex;
    flex-wrap: wrap;
    gap: 24px;
    box-sizing: border-box;
    margin: 0;
    padding: 0;
    list-style: none;
`;
